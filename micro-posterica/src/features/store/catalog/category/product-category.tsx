import { Outlet } from "react-router";

const ProductCategoryApp = () => {
  return (
    <div className="product-category-app">
      <Outlet />
    </div>
  );
};

export default ProductCategoryApp;
