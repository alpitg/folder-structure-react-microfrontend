type ProductFormAppProps = {
  mode: "add" | "edit";
};

const ProductFormApp = ({ mode }: ProductFormAppProps) => {
  return <div className="product-form-app">hi</div>;
};

export default ProductFormApp;
