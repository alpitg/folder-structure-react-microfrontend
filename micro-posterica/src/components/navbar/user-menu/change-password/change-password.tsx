import ModelApp from "../../../ui/model/model";

const ChangePasswordApp: React.FC<{ show: boolean }> = ({ show }) => {
  return (
    <>
      <ModelApp show={show}>hidden</ModelApp>
    </>
  );
};

export default ChangePasswordApp;
